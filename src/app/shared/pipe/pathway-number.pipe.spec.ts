import {PathwayNumberPipe} from './pathway-number.pipe';

describe('PathwayNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new PathwayNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
